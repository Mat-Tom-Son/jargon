{
  "name": "translation-layer",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "echo \"Run dev commands in sub-packages\"",
    "restart": "./restart-server.sh",
    "gateway": "cd apps/gateway && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "check:config": "node scripts/check-config.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
